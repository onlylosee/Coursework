<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:background">?android:attr/colorBackground</item>
    </style>

    <style name="AppTheme.Light" parent="AppTheme">
        <item name="android:background">@android:color/white</item>
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="android:textColor">@android:color/black</item> <!-- Явный цвет текста для светлой темы -->
    </style>

    <style name="AppTheme.Dark" parent="AppTheme">
        <item name="android:background">@android:color/black</item>
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="android:textColor">@android:color/white</item> <!-- Явный цвет текста для темной темы -->
    </style>
    <style name="AppThemeForList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@drawable/shapeschitem</item>
    </style>

    <style name="AppThemeForList.Light" parent="AppThemeForList">
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="android:textColor">@android:color/black</item> <!-- Явный цвет текста для светлой темы -->
    </style>

    <style name="AppThemeForList.Dark" parent="AppThemeForList">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="android:textColor">@android:color/white</item> <!-- Явный цвет текста для темной темы -->
    </style>
</resources>